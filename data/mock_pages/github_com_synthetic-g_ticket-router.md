# ticket-router
LLM-assisted support-ticket triage. intake → classify intent → route, with a
confidence threshold that escalates uncertain tickets to a human queue. ~300 commits
over a year, real README, a small eval set. Solid, real, owned work — matches what the
resume and portfolio describe. Nothing inflated about the artifact itself; the only
inflation is in how the resume *titles* and *frames* this work relative to LinkedIn.
